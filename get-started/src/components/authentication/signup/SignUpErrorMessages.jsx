export const emptyFieldsErrorMessage = () => {
  return <p class="error">Please fill in all fields before submitting</p>
}

export const invalidEmailErrorMessage = () => {
  return <p class="error">Please enter a valid email address</p>
}

export const passwordsDoNotMatchErrorMessage = () => {
  return <p class="error">Passwords do not match</p>
}

export const emailUsedErrorMessage = () => {
  return <p class="error">Email already in use</p>
}

export const passwordLengthErrorMessage = () => {
  return <p class="error">Password must be at least 6 characters</p>
}
